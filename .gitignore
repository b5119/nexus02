/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local agent identity/config — never commit device IDs or pairing secrets
.config/nexus/

# Android NDK gets installed locally, not vendored into the repo
.ndk/
*.apk
*.aab

# AI assistant working memory — prompts, session context, notes
# This is the assistant's scratchpad, not part of the public repo.
/PROMPTS.md
.claude/
